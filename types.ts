export type movieType = {
  filter: any;
  id: number,
  year: number,
  rating: number,
  title: string,
  poster: string,
  genres: string,
  summary: string,
}

export type itemNavbarType = {
  map: any;
  id: number,
  title: string,
  path: string,
}

export type headingType = {
  tag: string,
  className: string,
  text: string,
}